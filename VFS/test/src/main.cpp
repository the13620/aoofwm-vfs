#include <iostream>
#include <string>

#include "test.h"


int	unit(int ac, char **av)
{
	VFS::Util::URITestMain(ac, av);
	return (0);
}


int	vfs(int ac, char **av)
{
	
	return (0);
}


int	main(int ac, char **av)
{
	vfs(ac, av);
	return (0);	
}
